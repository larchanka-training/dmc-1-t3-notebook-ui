# Test environment — committed intentionally (no secrets).
# Vitest/Vite loads this in test mode, ensuring VITE_API_BASE_URL is an
# absolute URL so MSW node-transport handlers match API client requests.
# See: test/msw/handlers/auth.ts (TEST_API_BASE) and shared/api/config.ts.
VITE_API_BASE_URL=http://localhost:8000/api/v1
